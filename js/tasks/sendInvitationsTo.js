function sendInvitationsTo(friends) {
//  throw new Error("Error while sending invitations");
  for (var i = 0; i < friends.length; i++) {
    var friend = friends[i];
    console.log("sending invitation...", friend);
  }
}
